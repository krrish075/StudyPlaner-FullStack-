# MongoDB Connection String (e.g., mongodb://127.0.0.1:27017/smartstudyplanner or an Atlas URI)
MONGO_URI=mongodb://127.0.0.1:27017/smartstudyplanner

# JSON Web Token Secret (used for signing and verifying user auth tokens)
JWT_SECRET=super_secret_key_change_me_in_production

# Port for the Express server to run on (default 5000)
PORT=5000
